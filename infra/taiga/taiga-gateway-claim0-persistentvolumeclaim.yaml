apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: taiga-gateway-claim0
  name: taiga-gateway-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
